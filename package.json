{
  "name": "klanten-app",
  "version": "1.0.0",
  "description": "A simple klanten app",
  "main": "index.html",
  "scripts": {
    "start": "npx serve -p 3000",
    "dockerbuild": "docker build -t klanten-app ."
  },
  "dependencies": {
    "serve": "^14.2.1"
  }
}
